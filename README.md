## 🛠️ Installation
ตรวจสอบให้แน่ใจว่าเครื่องของคุณมีสิ่งเหล่านี้ก่อน
- **Node.js** (แนะนำเวอร์ชัน >= 22)  
  👉 https://nodejs.org
- **npm** (ติดมากับ Node.js)
- **Angular CLI** (เวอร์ชัน 21) ถ้ายังไม่มีให้ติดตั้งด้วยคำสั่ง 
  npm install -g @angular/cli

ตรวจสอบเวอร์ชัน:
```bash
node -v
npm -v
ng version

วิธีรันโปรเจค
1. Clone Repository
   git clone https://github.com/pissanupondev/job-test.git
   cd jobtest
2.Install Dependencies
   npm install
3.Run Development Server 
  npm start หรือใช้คำสั่ง ng serve

4.จากนั้นเปิด Browser ที่:
  http://localhost:4200


